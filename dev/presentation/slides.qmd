---
title: "The new PIP Auxiliary Data Pipeline"
subtitle: "The engine that keeps PIP's auxiliary data reproducible, versioned, stable and auditable"
author: "PIP Technical Team"
date: "June 2026"
format:
  revealjs:
    theme: [default, styles.scss]
    slide-number: c/t
    transition: slide
    background-transition: fade
    highlight-style: github-dark
    code-line-numbers: true
    fig-align: center
    width: 1400
    height: 900
    margin: 0.08
    center: true
    mermaid:
      theme: dark
---

# The Big Picture {.section-slide .center}

::: subtitle-text
What is the pipeline responsible for? How is it built?
:::

---

## One main orchestrator - `{pipaux}`

::: {style="font-size:0.85em; color:#94A3B8; max-width:820px; margin:0 auto 1em auto; text-align:center;"}
An R package that manages the 20+ auxiliary measures that feed the World Bank's global poverty estimates. For example, price data, national accounts, demographics and poverty measurement parameters.
:::

<div style="display:grid; grid-template-columns:1fr 1fr; gap:22px;">

<div style="font-size:0.9em; line-height:1.8; color:#CBD5E1; border-left:3px solid #334155; padding-left:16px;">
Takes [raw data from different sources]{.text-accent}, [cleans, validates, and formats]{.text-success} each one into [ready-to-use datasets]{.text-accent} for PIP's poverty estimates.
</div>

<div style="display:flex; flex-direction:column; gap:12px;">

<div style="background:rgba(56,189,248,0.07); border:1px solid rgba(56,189,248,0.25); border-radius:10px; padding:14px 18px;">
<div style="font-size:0.72em; letter-spacing:0.08em; text-transform:uppercase; color:#38BDF8; margin-bottom:6px;">Responsibility 1</div>
<div style="font-weight:700; font-size:0.95em; color:#E2E8F0; margin-bottom:5px;">Update & Synchronization Pipeline</div>
<div style="font-size:0.78em; color:#94A3B8; line-height:1.5;">Raw &rarr; validated &rarr; versioned artifacts on the Y-drive — dependency-ordered, rebuilt only when something changed.</div>
</div>

<div style="background:rgba(52,211,153,0.07); border:1px solid rgba(52,211,153,0.25); border-radius:10px; padding:14px 18px;">
<div style="font-size:0.72em; letter-spacing:0.08em; text-transform:uppercase; color:#34D399; margin-bottom:6px;">Responsibility 2</div>
<div style="font-weight:700; font-size:0.95em; color:#E2E8F0; margin-bottom:5px;">Comparison Engine</div>
<div style="font-size:0.78em; color:#94A3B8; line-height:1.5;">Structured diff reports across releases and within-release vintages.</div>
</div>


</div>
</div>

---

## Objectives

<div style="display:grid; grid-template-columns:1fr 1fr; gap:10px; margin-top:0.4em;">

<div style="background:rgba(56,189,248,0.07); border:1px solid rgba(56,189,248,0.2); border-left:3px solid #38BDF8; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0;">1</span>
<div><span class="text-accent" style="font-weight:600;">Consistent raw data management</span><br><span style="font-size:0.8em; color:#94A3B8;">GitHub as a reliable, release-linked staging area for all raw inputs</span></div>
</div>

<div style="background:rgba(56,189,248,0.07); border:1px solid rgba(56,189,248,0.2); border-left:3px solid #38BDF8; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0;">2</span>
<div><span class="text-accent" style="font-weight:600;">Automated processing for every measure</span><br><span style="font-size:0.8em; color:#94A3B8;">Pipelines triggered only when needed — avoiding redundant rebuilds while never missing a required update</span></div>
</div>

<div style="background:rgba(52,211,153,0.07); border:1px solid rgba(52,211,153,0.2); border-left:3px solid #34D399; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#34D399;">3</span>
<div><span class="text-success" style="font-weight:600;">Dependency-aware execution</span><br><span style="font-size:0.8em; color:#94A3B8;">A dependency graph that resolves order and propagates changes automatically</span></div>
</div>

<div style="background:rgba(52,211,153,0.07); border:1px solid rgba(52,211,153,0.2); border-left:3px solid #34D399; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#34D399;">4</span>
<div><span class="text-success" style="font-weight:600;">Double-stage data validation</span><br><span style="font-size:0.8em; color:#94A3B8;">Validation checks at both input and output stages for every measure</span></div>
</div>

<div style="background:rgba(251,191,36,0.07); border:1px solid rgba(251,191,36,0.2); border-left:3px solid #FBBF24; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#FBBF24; color:#0D1B2A;">5</span>
<div><span class="text-warning" style="font-weight:600;">Release-scoped outputs</span><br><span style="font-size:0.8em; color:#94A3B8;">Every output file is explicitly tied to a specific production release</span></div>
</div>

<div style="background:rgba(251,191,36,0.07); border:1px solid rgba(251,191,36,0.2); border-left:3px solid #FBBF24; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#FBBF24; color:#0D1B2A;">6</span>
<div><span class="text-warning" style="font-weight:600;">Versioning and traceability</span><br><span style="font-size:0.8em; color:#94A3B8;">Every version of every dataset is saved with full metadata — always accessible, always traceable</span></div>
</div>

<div style="background:rgba(129,140,248,0.07); border:1px solid rgba(129,140,248,0.2); border-left:3px solid #818CF8; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#818CF8;">7</span>
<div><span style="font-weight:600; color:#A5B4FC;">Full audit logging & error resilience</span><br><span style="font-size:0.8em; color:#94A3B8;">Every step is logged — successes, failures, and errors — so the pipeline never fails silently</span></div>
</div>

<div style="background:rgba(129,140,248,0.07); border:1px solid rgba(129,140,248,0.2); border-left:3px solid #818CF8; border-radius:8px; padding:10px 14px; display:flex; gap:12px; align-items:flex-start;">
<span class="step-badge" style="flex-shrink:0; background:#818CF8;">8</span>
<div><span style="font-weight:600; color:#A5B4FC;">Cross-release comparison</span><br><span style="font-size:0.8em; color:#94A3B8;">Tools that surface what changed, where, and when — across releases and vintages</span></div>
</div>

</div>

---

## Dependency Graph {.slide-top background-iframe="dependency-graph.html" background-interactive=true}

---


## The Ecosystem at a Glance

```{mermaid}
%%| fig-width: 13
%%| fig-height: 6.5
graph TB
    subgraph SRC["Data Sources"]
        direction TB
        GH["GitHub aux_* repos\n20+ measures"]
        CLASS["GPID-WB/Class\ncountry_list, income_groups"]
        EXT["External\nWDI · WEO · Maddison"]
    end

    subgraph ENGINE["pipaux Orchestration Engine"]
        direction TB
        DEP["Dependency Graph\nread_dependencies()"]
        CHECK["Status Checker\ncheck_github_status()\ncheck_y_drive_status()"]
        EXEC["Update Executor\nexecute_update()"]
        VAL["Validation Layer\n*_validate_raw()\n*_validate_output()"]
    end

    subgraph STORE["Version & Provenance"]
        direction TB
        STAMP["stamp versioning\ncontent-based"]
        SIDE["Sidecar metadata\nGH SHA · code_hash · pk"]
    end

    subgraph OUT["Published Outputs"]
        direction TB
        YDRIVE["Y-Drive release artifacts\n.qs2 files"]
        GBRANCH["GitHub release branches\nYYYYMMDD_PROD / TEST"]
    end

    subgraph AUDIT["Audit & Comparison"]
        direction LR
        LOG["Audit log\npipfun::log_*"]
        CMP["Comparison tools\ncompare_aux_releases()\ncompare_aux_vintages()"]
    end

    SRC --> ENGINE
    ENGINE --> STORE
    STORE --> OUT
    OUT --> AUDIT

    style ENGINE fill:#0B2540,stroke:#38BDF8,stroke-width:2px
    style SRC    fill:#0D2137,stroke:#475569
    style STORE  fill:#0D2137,stroke:#475569
    style OUT    fill:#0D2137,stroke:#475569
    style AUDIT  fill:#0D2137,stroke:#475569
```

::: {.text-muted}
The orchestration engine sits at the centre — it coordinates everything from source retrieval to publication.
:::

---

# End-to-End Workflow {.section-slide .center}

::: subtitle-text
How data moves from source to published artifact — eight stages
:::

---

## Pipeline Stages

```{mermaid}
%%| fig-width: 13
%%| fig-height: 5.8
flowchart LR
    S1["① Source\nGitHub aux_* repos\nExternal URLs"]
    S2["② Status Evaluation\nGH branch in sync?\nSHA / code hash match?"]
    S3["③ Dependency Analysis\nDependency manifest\nTopological resolution"]
    S4["④ Load & Process\nload_from_gh()\naux_*() formatter"]
    S5["⑤ Validation\nRaw validators\nOutput validators"]
    S6["⑥ Version Creation\nstamp content-based\nSidecar with provenance"]
    S7["⑦ Logging\nAudit entry per step\nTimestamp · status · SHA"]
    S8["⑧ Publication\nY-Drive .qs2\nGH release branch"]

    S1 --> S2
    S2 -->|update needed| S3
    S2 -->|up to date| SKIP(["✓ Skip"])
    S3 --> S4
    S4 --> S5
    S5 --> S6
    S6 --> S7
    S7 --> S8

    style SKIP fill:#134e2e,stroke:#34D399,color:#A7F3D0
    style S2   fill:#0B2540,stroke:#38BDF8
```

::: {.columns}
::: {.column width="55%"}
**Key principle — skip logic is as important as the update logic.**

The system only does work when something actually changed. A typical run where no sources have changed completes with every measure skipped — zero writes, full auditability.
:::
::: {.column width="45%"}
**What flows between stages:**

- S1 → S2: raw file SHA from GH
- S2 → S3: `update_gh` / `update_y` flags
- S3 → S4: ordered dependency list
- S4 → S5: raw data + `attributes(x)$gh`
- S5 → S6: validated data + metadata
- S6 → S8: versioned artifact + sidecar
:::
:::

---

## Reproducibility Through the Pipeline

Every artifact on the Y-drive carries a **sidecar** — a companion metadata file that makes it self-describing.

::: {.columns}
::: {.column width="48%"}
```
measure.qs2          ← the data
measure.qs2.sidecar  ← the provenance record

Sidecar contains:
  gh:
    owner:      "PIP-Technical-Team"
    repo:       "aux_cpi"
    branch:     "20260202_PROD"
    file_path:  "InputData/cpi.csv"
    gh_raw_sha: "a3f8c2..."     ← GH file fingerprint

  code_hash:  "d9e1b7..."       ← aux_cpi() body hash
  code_label: "aux_cpi"        ← function name
  pk:         ["country_code", "year"]
```
:::
::: {.column width="52%"}
**Two questions you can always answer:**

> *"What raw data version produced this file?"*

→ Compare `gh_raw_sha` against current GitHub SHA

> *"Did the formatting logic change since this was saved?"*

→ Compare `code_hash` against `hash_code(aux_cpi)`

Both comparisons happen automatically on every run — triggering updates only when needed.
:::
:::

---

# The Orchestration Engine {.section-slide .center}

::: subtitle-text
`aux_fun()` and `update_aux_measures()` — the central coordinators
:::

---

## Two Entry Points

::: {.columns}
::: {.column width="48%"}
**`aux_fun()` — single measure**

```r
aux_fun(
  measure = "cpi",
  verbose = TRUE
)
```

- Resolves dependencies recursively
- Checks GH + Y-drive status
- Executes only what's needed
- Initialises + finalises the audit log when called from the top level

Use when: updating one measure interactively, or running a targeted update.
:::
::: {.column width="4%"}
:::
::: {.column width="48%"}
**`update_aux_measures()` — batch**

```r
update_aux_measures(
  measures = c("cpi", "gdp"),
  log      = TRUE,
  log_save = TRUE
)
```

- Reads dependency manifest
- Preserves topological order
- Shares a single `processed` environment across all measures
- Prevents double-processing of shared dependencies

Use when: running a full release update.
:::
:::

---

## How the Engine Decides: Three Signals

Before any data is written, three checks run in sequence:

::: {.columns}

::: {.column width="32%"}
**① GitHub Branch Sync**

Is the release branch on the `aux_*` repo in sync with `DEV`?

If branch is missing or behind DEV → sync it.

[Policy:]{.text-warning} GH update always implies Y-drive update.
:::

::: {.column width="32%"}
**② GitHub SHA Comparison**

Does the raw file SHA stored in the Y-drive sidecar match the current SHA on GitHub?

Any mismatch → re-run the formatter.
:::

::: {.column width="32%"}
**③ Code Hash Comparison**

Did the `aux_*()` function body change since the last save?
Any mismatch → re-run, re-save.

[This catches silent logic changes]{.text-accent} even when raw data is unchanged.
:::

:::

---

## The Decision Matrix

| GH branch status | SHA match | Code hash match | Action |
|---|---|---|---|
| Missing / behind DEV | — | — | Sync GH → update Y-drive |
| In sync | ✗ mismatch | — | Update Y-drive |
| In sync | ✓ match | ✗ mismatch | Update Y-drive |
| In sync | ✓ match | ✓ match | [Skip — nothing to do]{.text-success} |
| Skipped (`countries`, `missing_data`) | ✓ match | ✓ match | [Skip]{.text-success} |
| Sidecar missing | — | — | Update Y-drive |

::: {.callout-note}
**The skip case is the common case.** In a stable release where no sources changed and no formatter functions were modified, every measure in the pipeline completes with zero writes.
:::

---

# Dependency Graph {.section-slide .center}

::: subtitle-text
How measures relate, and how the graph drives execution order
:::

---
**What the algorithm guarantees:**

- No measure is processed before its dependencies
- No measure is processed twice (the `processed` environment acts as a visited-node set)
- A failure in a dependency is logged and controlled by `halt_on_dep_fail`


:::
:::

---

## Execution Order Algorithm

`update_aux_measures()` resolves order before starting any work:

::: {.columns}
::: {.column width="50%"}
```r
# 1. Read manifest (dependency-ordered list)
available_measures <- names(
  read_dependencies(
    gh_user = "https://raw.githubusercontent.com",
    owner   = "PIP-Technical-Team"
  )
)

# 2. Subset to requested measures,
#    preserving manifest order
final_measures <- available_measures[
  available_measures %in% measures
]

# 3. Shared processed environment
processed <- new.env(parent = emptyenv())

# 4. Loop — each call to aux_fun() marks
#    its own dependencies as processed
for (measure in final_measures) {
  aux_fun(measure, processed = processed, ...)
}
```
:::
::: {.column width="50%"}
**Why a shared `processed` environment?**

When you request `c("cpi", "gdp")`:

- Both depend on `country_list`
- Without a shared visited-node set, `country_list` would be checked (and potentially updated) twice
- The `processed` env is passed by reference into every recursive `aux_fun()` call
- Once `country_list` is marked, all subsequent measures skip it immediately

This is the mechanism that prevents duplicate work across a batch run.
:::
:::

---

# Recursive Update Workflow {.section-slide .center}

::: subtitle-text
A detailed look at what happens inside a single `aux_fun()` call
:::

---

## The Full Algorithm

```{mermaid}
%%| fig-width: 12
%%| fig-height: 7.5
flowchart TD
    A(["aux_fun(measure)"])
    B{"is_top_level()?"}
    C["init_aux_log()\ncreate processed env"]
    D["resolve repo / owner\nresolve_measure_repo_owner()"]
    E["read_dependencies()\nget deps list for measure"]
    F{"For each dependency:\nalready in processed env?"}
    G["Mark dep as processed\nenv_poke(processed, dep, TRUE)"]
    H(["aux_fun(dep)\nRecurse"])
    I["log_add START for measure"]
    J["get_fs_status()\ncheck GH + Y-drive"]
    K{"update_gh\nor update_y?"}
    L(["log NO UPDATE\nreturn invisible(NULL)"])
    M{"update_gh\n= TRUE?"}
    N["sync_release_branch()\nDEV to release_branch\nlog UPDATE_GH"]
    O["aux_*(action='update',...)\nload validate save\nlog UPDATE_Y"]
    P["log SUCCESS"]
    Q{"is_top_level()\n&& log?"}
    R["finalize_aux_log()"]
    DONE(["Done"])

    A --> B
    B -->|Yes| C --> D
    B -->|No| D
    D --> E --> F
    F -->|Yes skip| F
    F -->|No| G --> H --> F
    F -->|All done| I --> J --> K
    K -->|Neither TRUE| L
    K -->|At least one TRUE| M
    M -->|Yes| N --> O --> P
    M -->|No| O --> P
    P --> Q
    Q -->|Yes| R --> DONE
    Q -->|No| DONE

    style L fill:#134e2e,stroke:#34D399,color:#A7F3D0
    style H fill:#1e3a5f,stroke:#38BDF8,color:#BAE6FD
    style N fill:#2d1b69,stroke:#818CF8,color:#C7D2FE
    style O fill:#2d1b69,stroke:#818CF8,color:#C7D2FE
```

---

## What the Y-Drive Update Actually Does

When `update_y = TRUE`, `execute_update()` calls the measure's generator function:

::: {.columns}
::: {.column width="50%"}
```r
# Dynamically resolve the function
func_name <- paste0("aux_", measure)
func <- get(func_name,
            envir = asNamespace("pipaux"))

# Build args — only pass what the
# function actually accepts
update_args <- list(
  action = "update",
  branch = release_branch,
  owner  = owner,
  tag    = tag,
  repo   = repo
)

filtered_args <- update_args[
  names(update_args) %in%
    names(formals(func))
]

do.call(func, filtered_args)
```
:::
::: {.column width="50%"}
**Inside each `aux_*()` function:**

1. `pipfun::load_from_gh(...)` — fetch raw data, captures `attributes(x)$gh`
2. `*_validate_raw(x)` — validate incoming structure
3. Format / clean / join
4. `*_validate_output(x)` — validate result
5. `pip_aux_save(x, metadata = list(gh = ...), code = ..., pk = ...)` — write to Y-drive with full provenance

**The critical invariant:**  
`attributes(x)$gh` must be present on everything that flows into `pip_aux_save()`. This is what enables SHA-based change detection on the next run.
:::
:::

---

## Handling Derived Measures

Some measures don't read from GitHub directly — they are computed from other aux data:

::: {.columns}
::: {.column width="48%"}
**Example: `aux_countries`**

```r
# Load from already-updated measures
cl  <- pipload::load_aux_data("country_list")
pfw <- pipload::load_aux_data("pfw")

# Subset: countries in pfw with inpovcal == 1
result <- cl[
  country_code %in%
    pfw[inpovcal == 1, country_code]
]

# Aggregate GH metadata from inputs
combined_gh <- list(
  attributes(cl)$gh,
  attributes(pfw)$gh
)

pip_aux_save(result,
  metadata   = list(gh = combined_gh),
  code       = aux_countries,
  code_label = "aux_countries",
  pk         = "country_code"
)
```
:::
::: {.column width="52%"}
**Change detection for derived measures:**

Since there is no single raw file, change detection relies on:

- [Code hash:]{.text-accent} if `aux_countries()` body changes → re-run
- [Inherited GH SHAs:]{.text-accent} `combined_gh` carries the original raw file SHAs from `country_list` and `pfw`

On the next run, `check_y_drive_status()` iterates over every entry in `sidecar$gh` and compares each against its current GitHub SHA.

If `country_list` was updated upstream, its SHA changed → `aux_countries` detects the mismatch and re-runs automatically — even though it never reads directly from GitHub.

[This is the cascade mechanism.]{.text-warning}
:::
:::

---

# Supporting Infrastructure {.section-slide .center}

::: subtitle-text
Versioning · Logging · Comparison tools
:::

---

## Versioning with `stamp`

`{pipaux}` uses the `stamp` package for content-aware artifact versioning.

::: {.columns}
::: {.column width="48%"}
**Three modes:**

```r
# Default — new version only when
# content or code changes
pipaux_set_versioning("content")

# Force new version every run
# (useful for debugging)
pipaux_set_versioning("timestamp")

# Overwrite in place, no history
pipaux_set_versioning("off")
```

**pipaux defaults** (`pipaux_reset_stamp_options()`):

- `versioning = "content"`
- `retain_versions = Inf`
- `force_on_code_change = TRUE`
- `code_hash = TRUE`
:::
::: {.column width="52%"}
**What content-based versioning means in practice:**

Each time `pip_aux_save()` is called, `stamp` computes a hash of the data. If the hash matches the current stored version, no new version is created.

Versions are stored under:
```
Y:/PIP_ingestion_pipeline_v2/
  20260202_PROD/
    aux_data/
      cpi.qs2              ← current
      cpi.qs2.sidecar      ← current provenance
      cpi.qs2/
        versions/
          v1_20260115/     ← prior version
          v2_20260202/     ← this version
```

Within a release, you can always roll back or compare vintages without touching the main file.
:::
:::

---

## Logging Framework

Every run produces a structured audit trail.

::: {.columns}
::: {.column width="50%"}
**Log lifecycle:**

1. `init_aux_log()` — called at top-level entry
2. `log_add(event, message, logmeta)` — called at each step
3. `finalize_aux_log()` — called on exit via `on.exit()`
4. Optionally: `pipfun::log_save()` persists to Y-drive

**Accessing logs:**

```r
# Quick summary table
aux_log_summary()
#      measure   status
# 1:       cpi  success
# 2:       ppp  success
# 3:       gdp    error

# Full structured log
aux_log_last()

# Load a saved log from disk
pipfun::log_load(
  id    = aux_log_last_name(),
  alias = get_from_auxenv("aux_meta_alias")
)
```
:::
::: {.column width="50%"}
**Each log entry captures:**

```
→ [2026-05-28 13:59:18] INFO
  message:  "Starting status check for: cpi"
  step:     "START"
  measure:  "cpi"

→ [2026-05-28 14:01:42] UPDATE
  message:  "Updated Y drive for: cpi"
  step:     "UPDATE_Y"
  measure:  "cpi"

→ [2026-05-28 14:01:45] SUCCESS
  message:  "Measure and dependencies
             successfully updated"
  step:     "END"
  measure:  "cpi"
```

Log steps: `START` → `UPDATE_GH` → `UPDATE_Y` → `END`  
Error steps: `ERROR_CHECK` · `ERROR_GH` · `ERROR_Y` · `ERROR_DEP`
:::
:::

---

## Comparison Tools

After updating, two functions let you inspect what changed.

::: {.columns}
::: {.column width="48%"}
**`compare_aux_releases()`**

*"What changed between this release and last?"*

```r
changes <- compare_aux_releases(
  measure     = c("cpi", "gdp"),
  old_release = "20260101_PROD"
)

# Cell-level differences
changes$cpi$diff_values
# Rows added / removed
changes$cpi$diff_rows
```

Compares the current release's `.qs2` artifact against the most recent previous release of the same identity.

Automatically detects `old_release` if not supplied.
:::
::: {.column width="52%"}
**`compare_aux_vintages()`**

*"Did my re-run within this release change anything?"*

```r
vintage_changes <- compare_aux_vintages(
  measures = c("cpi", "gdp"),
  version  = -1   # vs immediately prior version
)
```

Compares version `N` against version `N + version` within the same release directory.

**Both return:**

| Field | Contents |
|---|---|
| `diff_values` | Cell-level diffs (`.x` = new, `.y` = old) |
| `diff_rows` | Added / removed rows with `change_type` |

Attributes carry: `key_cols`, `release`, `old_release`, `new_path`, `old_path`.
:::
:::

---

# Quick Start {.section-slide .center}

::: subtitle-text
The complete workflow in four commands
:::

---

## Full Workflow

```r
# ── 1. Initialise the runtime environment ─────────────────────────────
pipfun::setup_working_release(release = "20260601", identity = "TEST")
devtools::load_all()

# Verify
get_from_auxenv("wrk_release")
#> $release   "20260601"
#> $identity  "TEST"

# ── 2. Update measures (dependencies resolved automatically) ──────────
update_aux_measures(
  measures = c("cpi", "ppp", "gdp"),
  owner    = "RossanaTat",   # use personal fork for testing
  log      = TRUE,
  log_save = TRUE
)
# ✔ No updates needed         ← common case when nothing changed
# ✔ Updated GitHub for: cpi   ← when release branch was behind DEV
# ✔ Updated Y drive for: cpi  ← when SHA or code hash differed

# ── 3. Inspect the audit log ──────────────────────────────────────────
aux_log_summary()

# ── 4. Verify nothing unexpectedly changed ────────────────────────────
compare_aux_releases(measure = c("cpi", "gdp"))
```

::: {.callout-tip}
Always call `pipfun::setup_working_release()` before any update or read operation. It populates the `.pipaux` runtime environment with paths, release metadata, and options.
:::

---

## Checking Status Without Updating

When you want to understand the current state without triggering any writes:

```r
# Full status check with reasons
check_status("maddison", include_reason = TRUE)

# ── Checking Status for maddison ────────────────────────────────────
# ✔ GitHub branch 20260601_TEST is up to date with DEV.
# ✔ Current and stored function SHAs match.
# ── Summary ─────────────────────────────────────────────────────────
# ℹ Update GitHub: FALSE
# ℹ Update Y drive: FALSE
```

```r
# Or use the quiet wrapper for programmatic use
status <- get_fs_status("cpi", include_reason = TRUE)
status$update_y    #> TRUE
status$y_reason    #> "Y drive out of sync"
```

`check_status()` and `get_fs_status()` are used internally by `aux_fun()` — but they are also useful standalone diagnostic tools when you need to understand why a measure is (or isn't) being updated.

---

# Summary {.section-slide .center}

---

## What `{pipaux}` Does

::: {.columns}
::: {.column width="55%"}
**System capabilities:**

- Manages 20+ auxiliary measures with a single call
- Resolves dependency order automatically from a manifest
- Detects changes via three independent signals (GH branch, file SHA, code hash)
- Saves every artifact with full provenance (sidecar)
- Versions content — writes only when something changed
- Produces structured audit logs for every run
- Compares releases and vintages to surface what changed

**Architecture:**

`aux_fun()` → `process_dependencies()` → `get_fs_status()` → `execute_update()` → `pip_aux_save()`
:::
::: {.column width="45%"}
**Three key design principles:**

::: {.callout-tip}
**Fail loudly** — explicit errors for missing data, sidecar, or GH artifacts. No silent fallbacks.
:::

::: {.callout-note}
**Commit provenance** — `attributes(x)$gh` must survive through the entire pipeline from load to save.
:::

::: {.callout-warning}
**Dependency-first** — all upstream measures are resolved and marked processed before a measure updates itself.
:::
:::
:::

---

## {.center}

::: {r-fit-text}
`update_aux_measures()`
:::

::: {.text-muted style="text-align:center; margin-top: 0.5em;"}
One call. All dependencies. Full provenance. Complete audit trail.
:::
