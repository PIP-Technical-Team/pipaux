% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_changes.R
\name{compare_aux_version_ids}
\alias{compare_aux_version_ids}
\title{Compare two auxiliary data versions by explicit version IDs}
\usage{
compare_aux_version_ids(
  measure,
  new_version_id,
  old_version_id,
  verbose = FALSE
)
}
\arguments{
\item{measure}{Character. The name of the auxiliary measure to compare
(e.g., \code{"cpi"}, \code{"gdp"}).}

\item{new_version_id}{Character scalar. Version ID (hash) to use as the
newer/reference dataset.}

\item{old_version_id}{Character scalar. Version ID (hash) to use as the
older/comparison dataset.}

\item{verbose}{Logical. If \code{TRUE}, displays informative messages in the
console. Default is \code{FALSE}.}
}
\value{
Invisibly returns a named list containing:
\describe{
\item{diff_values}{A data table of value-level differences across matched
rows and columns. Columns with \code{.x} suffix refer to \code{new_version_id};
\code{.y} suffix refers to \code{old_version_id}. \code{NULL} if no differences found.}
\item{diff_rows}{A data table of rows added or removed between versions,
with \code{change_type} column (\code{"added"} = in \code{new_version_id} only,
\code{"removed"} = in \code{old_version_id} only). \code{NULL} if no row differences found.}
}

The list also has attributes:
\describe{
\item{key_cols}{Character vector of primary key columns used for comparison.}
\item{measure}{Character string of the measure name.}
\item{new_version_id}{Character string of the newer version ID.}
\item{old_version_id}{Character string of the older version ID.}
\item{new_path}{Character string path to the newer version snapshot directory.}
\item{old_path}{Character string path to the older version snapshot directory.}
\item{release}{Character string of the current release identifier.}
}
}
\description{
Loads two specific versions of the same auxiliary measure using explicit
version IDs (hashes) and compares them using the same \code{myrror}-based logic
used by vintage comparisons.
}
\examples{
\dontrun{
compare_aux_version_ids(
  measure = "cpi",
  new_version_id = "v_01J9ABCDEF123",
  old_version_id = "v_01J8UVWXYZ456"
)
}
}
\seealso{
\code{\link[=compare_vintage_versions]{compare_vintage_versions()}}, \code{\link[pipload:load_aux_data]{pipload::load_aux_data()}},
\code{\link[myrror:myrror]{myrror::myrror()}}
}
