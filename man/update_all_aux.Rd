% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aux_fun.R
\name{update_all_aux}
\alias{update_all_aux}
\title{Automatically Update All Auxiliary Data Files}
\usage{
update_all_aux(
  measures = NULL,
  verbose = FALSE,
  log = TRUE,
  log_save = FALSE,
  ...
)
}
\arguments{
\item{measures}{A character vector of auxiliary data measures to update.
If \code{NULL}, all known measures will be updated.}

\item{verbose}{Logical; whether to print informative messages to the console.}

\item{log}{Logical; whether to log the update process.}

\item{log_save}{Logical; whether to save the log file to disk.}

\item{...}{
  Arguments passed on to \code{\link[=aux_fun]{aux_fun}}
  \describe{
    \item{\code{repo}}{character: Name of the GitHub repository containing the auxiliary data
Defaults to \code{"aux_<measure>"} unless \code{measure} is \code{"income_groups"} or \code{"country_list"}, in which case it defaults to \code{"Class"}.}
    \item{\code{owner}}{character: GitHub repository owner. Default is \code{getOption("pipfun.ghowner")}}
    \item{\code{processed}}{environment: Environment that keeps track of already processed measures to avoid redundant processing.
Default is a new empty environment}
    \item{\code{force}}{logical: If \code{TRUE}, forces an update even if the data appears up to date.
Default is \code{FALSE}}
    \item{\code{tag}}{character: Specifies the GitHub branch tag for versioning.
Defaults to \code{release_branch}, which is determined from the working release}
  }}
}
\value{
A list containing the update status for each measure. Each element
is a list with the components: \code{measure} (character), \code{success} (logical),
and \code{error} (character or \code{NULL}).
}
\description{
This function updates all or a selected subset of auxiliary data measures,
ensuring that both the corresponding GitHub repositories and the network
drive (Y drive) are synchronized.
}
\details{
When \code{measures = NULL}, all known auxiliary data measures (i.e., all GitHub
repos starting with \code{"aux_"}) are processed. Otherwise, provide a character
vector of specific measures to process selectively.

The function logs the update status, captures any errors, and optionally
saves the log to a file. This is useful for tracking the status of each
update and debugging if needed.
}
\examples{
\dontrun{
# Update all measures and save the log
update_all_aux(log = TRUE, log_save = TRUE)

# Update only specific measures
update_all_aux(measures = c("cpi", "gdp"), verbose = TRUE)
}

}
\seealso{
\code{\link[=aux_fun]{aux_fun()}} for the function that performs the update of a single measure.
}
