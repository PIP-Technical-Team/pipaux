% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stamp_options.R
\name{pipaux-stamp-options}
\alias{pipaux-stamp-options}
\title{pipaux: Helper wrappers for stamp versioning options}
\description{
Small helpers to manage \code{stamp}-based versioning behaviour used by pipaux.
These functions provide a simple, stable API for:
\itemize{
\item switching between common versioning modes,
\item reading the active mode, and
\item resetting stamp options to the pipaux defaults.
}

They intentionally keep the surface small and forward arguments to \code{stamp}
so the package can adopt stamp improvements without internal changes.
}
\details{
pipaux relies on the \code{stamp} package to manage auxiliary-data versioning.
The three supported high-level modes are:
\itemize{
\item "content": only create a new version when data content or code changes (default)
\item "timestamp": create a new version on every save
\item "off": overwrite the current artifact (no versioning)
}

Use \code{pipaux_set_stamp_option()} to forward lower-level, advanced options
directly to \code{stamp::st_opts()} when you need finer control.
}
\examples{
\dontrun{
# set behaviour to create a new version only when content or code changes
pipaux_set_versioning("content")

# set behaviour to always create a new version (useful for debugging)
pipaux_set_versioning("timestamp")

# temporarily disable versioning (overwrites files)
pipaux_set_versioning("off")

# query current mode
pipaux_get_versioning()

# set a lower-level stamp option (advanced)
pipaux_set_stamp_option(force_on_code_change = FALSE)

# reset to pipaux defaults
pipaux_reset_stamp_options()
}

}
