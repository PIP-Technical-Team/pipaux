% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_aux_data.R
\name{aux_fun}
\alias{aux_fun}
\title{Update auxiliary data for a measure and its dependencies}
\usage{
aux_fun(
  measure,
  repo = NULL,
  owner = getOption("pipfun.ghowner"),
  processed = new.env(parent = emptyenv()),
  tag = NULL,
  log = TRUE,
  log_overwrite = TRUE,
  verbose = FALSE,
  halt_on_dep_fail = FALSE,
  log_name = NULL
)
}
\arguments{
\item{measure}{Character. Name of the auxiliary data measure to update
(e.g., \code{"cpi"}, \code{"ppp"}).}

\item{repo}{Character or \code{NULL}. GitHub repository name. Defaults to
\code{"aux_<measure>"} if \code{NULL}.}

\item{owner}{Character. GitHub repository owner. Defaults to
\code{getOption("pipfun.ghowner")}.}

\item{processed}{Environment. Tracks already-processed measures to avoid
cyclic dependency updates. Defaults to a fresh empty environment.}

\item{tag}{Character or \code{NULL}. Release tag passed to the generator
function. Defaults to the release branch name if \code{NULL}.}

\item{log}{Logical. If \code{TRUE}, audit log events are recorded.
Default is \code{TRUE}.}

\item{log_overwrite}{Logical. If \code{TRUE}, overwrites an existing log with
the same name. Default is \code{TRUE}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Default is
\code{FALSE}.}

\item{halt_on_dep_fail}{Logical. If \code{TRUE}, stops execution when a
dependency update fails. Default is \code{FALSE}.}

\item{log_name}{Character or \code{NULL}. Leave it NULL.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Main entry point for updating a single auxiliary data measure. Resolves
the release branch from the working environment, processes upstream
dependencies via \code{\link[=process_dependencies]{process_dependencies()}}, checks whether updates are
needed via \code{\link[=get_fs_status]{get_fs_status()}}, and calls \code{\link[=execute_update]{execute_update()}} if required.
Top-level calls initialise and finalise the audit log automatically.
}
\examples{
\dontrun{
aux_fun(measure = "cpi")
aux_fun(measure = "ppp", verbose = TRUE)
}
}
